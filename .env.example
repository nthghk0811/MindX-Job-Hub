# Backend API URL for Production (set this on Vercel Dashboard)
# Example: VITE_API_URL=https://mindx-job-hub-backend.onrender.com/api
# Leave empty for local development (Vite reverse proxy will handle /api -> http://127.0.0.1:5000)
VITE_API_URL=
